{% extends 'base.html.twig' %}

{% block title %}Contacts{% endblock %}

{% block body %}
    <style>
        #about {
        margin-left: 50px;
        }
        #aboutP {
        margin-left: 100px;
        }
        #service {
        margin-left : 75px;
        }
    </style>

    <h1>Contacts</h1>
    <div>
        {% set entry = contacts | split(':') %}
        <h2 id='about'>Name</h2>
        <p id='aboutP'>{{entry[0]}}</p>
        <h2 id='about'>Email</h2>
        <p id='aboutP'>{{entry[1]}}</p>
    </div>
{% endblock %}
